export const _create = (url: string) => new URL(url)
export const hash = (url: URL) => url.hash
export const hostname = (url: URL) => url.hostname
export const href = (url: URL) => url.href
export const toString = (url: URL) => url.toString()
export const origin = (url: URL) => url.origin
export const password = (url: URL) => url.password
export const pathname = (url: URL) => url.pathname
export const port = (url: URL) => url.port
export const protocol = (url: URL) => url.protocol
export const search = (url: URL) => url.search
export const searchParams = (url: URL) => url.searchParams
export const username = (url: URL) => url.username
export const toJSON = (url: URL) => url.toJSON()
